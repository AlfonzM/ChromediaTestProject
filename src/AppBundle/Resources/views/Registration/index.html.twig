{% extends "::base.html.twig" %}

{% block title %}Registration{% endblock %}

{% block body %}
<h1>Sign up</h1>

{{ form_start(form) }}
	{{ form_row(form.firstName) }}
	{{ form_row(form.lastName) }}
	{{ form_row(form.email) }}
	{{ form_row(form.password) }}
{{ form_end(form) }}

<br>
<a href="{{ path('login') }}">Already have an account? Sign in!</a><br>
{% endblock %}
